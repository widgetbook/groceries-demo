import 'package:flutter/widgets.dart';

import '../../../ui/ui.dart';

class AccountErrorView extends StatelessWidget {
  const AccountErrorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const ViewShell.error();
  }
}
